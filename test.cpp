#include<bits/stdc++.h>
using namespace std;
using ll = long long;
using lli = long long int;

void solve(){
    string s; cin>>s;
    if(s.size()==1){
        cout<<"0 1";
        return ;
    }
    int n = s.size();

    // two pointers(?
    int i=0,j = 1;
    int count = 0;

    for(i=0; i-1 < n && j < n; i++){
        
    }


}

int main(){
    ios::sync_with_stdio(false); cin.tie(nullptr);
    
    int t=1; 
    // cin>>t;
    while(t--){solve();cout<<'\n';}

    return 0;
}